import { SectionSlot } from "@/components/SectionSlot";

export const metadata = { title: "Work" };

export default function WorkPage() {
  return (
    <SectionSlot
      id="work"
      title="Work"
      note="Project grid from Supabase. Define card layout, filters, and hover behavior when ready."
    />
  );
}
