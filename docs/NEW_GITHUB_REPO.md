# Split into GitHub repo SUDARVEL/Portfolio

Repo already created: https://github.com/SUDARVEL/Portfolio

If it is still empty, follow [PUBLISH_TO_GITHUB.md](./PUBLISH_TO_GITHUB.md) — push this branch tip as `main`:

```bash
git push https://github.com/SUDARVEL/Portfolio.git origin/cursor/portfolio-initial-7312:main
```

Then connect that repo to Vercel (Root Directory = `.`).

Supabase stays a **separate** project named `portfolio` — never reuse the Oncosmart fitness project keys here.
