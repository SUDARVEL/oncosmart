import type { ImageSource } from 'expo-image';

/** Bundled preview for the home day card — avoids looping a Supabase MP4 on every visit. */
export const HOME_CARD_PLACEHOLDER_IMAGE: ImageSource = require('../assets/workouts/diaphragmatic-breathing.png');
